[project]
name = "honeywatch"
version = "0.1.0"
description = "Honeypot-Based Attack Monitoring & Analysis Platform"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "ryutaro yamashita" }]

dependencies = [
    "fastapi==0.115.0",
    "uvicorn[standard]==0.30.6",
    "sqlalchemy[asyncio]==2.0.35",
    "alembic==1.13.3",
    "asyncpg==0.29.0",
    "pydantic==2.9.2",
    "pydantic-settings==2.5.2",
    "redis[hiredis]==5.1.1",
    "asyncssh==2.17.0",
    "aiohttp==3.10.8",
    "structlog==24.4.0",
    "httpx==0.27.2",
    "pyyaml==6.0.2",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.3",
    "pytest-asyncio==0.24.0",
    "pytest-cov==5.0.0",
    "httpx==0.27.2",
    "ruff==0.6.8",
    "mypy==1.11.2",
    "types-PyYAML==6.0.12.20240917",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/honeywatch"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "S",   # flake8-bandit (security)
    "N",   # pep8-naming
]
ignore = [
    "S104", # Possible binding to all interfaces (Honeypot は意図的に公開する)
]

[tool.ruff.lint.flake8-bugbear]
# FastAPI の Depends / Query / Path は引数デフォルトでの関数呼び出しが正当なパターン
extend-immutable-calls = [
    "fastapi.Depends",
    "fastapi.Query",
    "fastapi.Path",
]

[tool.ruff.lint.per-file-ignores]
# テストでは assert とダミー認証情報の使用を許容する
"tests/**/*.py" = ["S101", "S105", "S106", "S107"]

[tool.ruff.lint.isort]
known-first-party = ["honeywatch"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
mypy_path = "src"

[[tool.mypy.overrides]]
module = ["asyncssh.*", "redis.*"]
ignore_missing_imports = true

# redis.asyncio.from_url 等は型スタブが不完全なため untyped call を許容する
[[tool.mypy.overrides]]
module = ["honeywatch.collector.handler", "honeywatch.detection.ipcontext", "honeywatch.api.routes.health"]
disable_error_code = ["no-untyped-call"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
